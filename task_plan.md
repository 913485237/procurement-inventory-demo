# 采购与库存补货协同管理系统：可交付版本实施计划

## 目标

将现有本地演示系统收敛为可公开展示的采购业务 MVP，并准备 GitHub、线上体验和简历材料。项目必须保持“独立设计的个人项目”定位，使用虚构数据，不虚构企业客户或量化收益。

## 阶段

| 阶段 | 状态 | 验收标准 |
|---|---|---|
| 1. 交付基线审计 | complete | 确认当前启动、数据库、权限、采购闭环和测试现状；记录缺口 |
| 2. 采购主流程加固 | complete | 从库存风险进入补货方案，完成审批并生成采购单，状态和权限一致 |
| 3. 线上演示准备 | in_progress | 有稳定演示数据、恢复机制、错误反馈和部署说明；确认线上托管方案 |
| 4. GitHub 交付材料 | in_progress | README、流程图、截图、演示脚本和项目边界说明完整且无乱码 |
| 5. 验证与简历资产 | pending | 测试通过，五分钟演示可复现，形成简历项目描述和面试口径 |

## 当前决策

- 目标岗位：采购专员 / 供应链专员。
- 项目名称：采购与库存补货协同管理系统。
- 交付组合：GitHub 仓库 + 在线演示地址 + README + 简短演示视频。
- 核心闭环：库存风险识别 → 补货决策 → 采购审批 → 采购单 → 到货及订单影响跟踪。
- 补货方式：AI 建议与人工经验判断并存；无 AI 接口时使用本地规则引擎。
- 数据策略：仅使用虚构演示数据。

## 当前实施批次

### 批次 A：最小可交付包

- 让服务支持 `HOST` / `PORT` 环境变量，兼容常见云平台容器启动方式。
- 增加 Dockerfile 和 `.dockerignore`，保证线上部署不依赖本机环境。
- 重写 README 首屏，优先呈现采购岗位定位、线上体验入口、五分钟演示路径和项目边界。
- 新增采购岗位专用演示脚本，明确每一步点击和讲解重点。
- 保持现有 17 项测试和本地 SQLite 演示方式不变。

### 批次 B：公开演示模式

- 使用 `PUBLIC_DEMO=true` 开启公开演示模式，本地默认行为保持不变。
- 匿名访客仍可切换虚构角色，管理员角色仍可完成采购审批。
- 服务端强制禁止修改 AI 配置和重置演示数据，不能只依靠前端隐藏按钮。
- 健康接口返回公开演示状态，前端显示“仅供演示”并禁用相应控件。
- 增加自动化测试，分别验证公开模式拦截和本地模式兼容性。

状态：已完成。线上托管平台和实际公开地址仍待后续确定。

## 风险与待确认项

- 线上托管平台和域名尚未确定；部署前需确认是否允许公开演示地址。
- 当前项目 README 曾出现中文编码显示异常，需要按 UTF-8 重新检查。
- 当前 README 使用 UTF-8 读取正常；此前乱码来自 PowerShell 默认编码显示，不是文件内容损坏。
- 当前服务端以请求中的 `user_id` 识别角色，适合本地演示但不适合直接暴露为生产权限模型；线上演示需要增加演示模式边界或会话校验。
- 不新增与采购主线无关的大模块，避免项目范围膨胀。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---|---|
| 在仓库外执行 git 命令导致 not a git repository | 1 | 后续使用 `git -C C:\\网页\\频转` 指定仓库路径 |
| 本机未安装 Docker，无法执行镜像构建验证 | 1 | 已完成 Dockerfile 静态检查；部署时需在安装 Docker 的环境或云平台构建 |
| 清理视觉验收临时数据库时命令被执行策略拦截 | 1 | 已停止验收服务；临时数据库位于被 Git 忽略的 `data/` 目录，不影响项目或提交 |
| 本机未安装 GitHub CLI，应用内浏览器与 Edge 扩展控制 GitHub 表单持续超时 | 2 | 已确认账号登录并打开新仓库页面；请用户手动提交空公开仓库，随后使用标准 Git 关联和推送 |
